import SettingsForm from '../components/SettingsForm';

const Settings = () => {
  return (
    <div className="settings-page">
      <h2>Settings</h2>
      <SettingsForm />
    </div>
  );
};

export default Settings;
export const formatDate = (date) => {
  // Implement date formatting logic
  // Example: return date.toLocaleDateString();
};

export const truncateString = (str, length) => {
  // Implement string truncation logic
  // Example: return str.length > length ? str.slice(0, length) + '...' : str;
};

export const calculateAverage = (numbers) => {
  // Implement average calculation logic
  // Example: return numbers.reduce((sum, num) => sum + num, 0) / numbers.length;
};
